# Web界面使用说明

## 功能特性

- 🌐 Web可视化界面，方便配置和管理
- ⚙️ Token配置（用于未来API集成）
- 📜 翻译历史记录查看和管理
- 💾 使用MongoDB本地存储历史记录

## 安装步骤

### 1. 安装主项目依赖

**重要：必须先安装依赖才能使用！**

```bash
npm install
```

如果遇到网络问题，可以使用国内镜像：
```bash
npm install --registry=https://registry.npmmirror.com
```

### 2. 安装Web前端依赖

```bash
cd web
npm install
cd ..
```

如果遇到网络问题：
```bash
cd web
npm install --registry=https://registry.npmmirror.com
cd ..
```

### 3. 构建Web前端（生产环境）

```bash
npm run build
```

**注意：** 如果只是开发使用，可以跳过构建步骤，直接使用开发模式（见下方）。

### 4. 安装并启动MongoDB

**macOS (使用Homebrew):**
```bash
brew install mongodb-community
brew services start mongodb-community
```

**Linux:**
```bash
sudo apt-get install mongodb
sudo systemctl start mongodb
```

**Windows:**
下载并安装MongoDB Community Server，然后启动MongoDB服务。

## 使用方法

### 启动Web界面

**方式1: 使用命令行**
```bash
fanyi web
```

**方式2: 使用 npm scripts（推荐用于开发）**
```bash
npm run web
# 或
npm start
# 或
npm run start:web
```

然后在浏览器中打开 `http://localhost:3000`

**开发模式（前端热重载）**

如果需要开发前端界面，可以使用开发模式：

```bash
# 终端1: 启动后端服务器
npm run web

# 终端2: 启动前端开发服务器（支持热重载）
npm run dev:web
```

前端开发服务器会在 `http://localhost:3001` 运行，并自动代理 API 请求到后端服务器（`http://localhost:3000`）。修改前端代码后会自动刷新浏览器。

### Web界面功能

1. **配置页面**
   - 设置API Token（可选）
   - 设置源语言和目标语言
   - 保存配置

2. **历史记录页面**
   - 查看所有翻译历史
   - 删除单条记录
   - 清空所有记录

### CLI翻译（会自动保存历史）

```bash
fanyi apple
fanyi 你好世界
```

翻译结果会自动保存到MongoDB数据库中。

## 项目结构

```
fanyi-cli/
├── bin/
│   └── fanyi.js          # CLI入口文件
├── src/
│   └── translate.js      # 翻译核心逻辑
├── server/
│   └── index.js          # Express后端服务器
├── web/
│   ├── src/
│   │   ├── App.jsx       # React主组件
│   │   ├── App.css       # 样式文件
│   │   └── main.jsx      # React入口
│   ├── index.html
│   └── package.json      # Web前端依赖
└── package.json          # 主项目依赖
```

## 注意事项

- MongoDB必须运行才能保存历史记录
- 如果MongoDB未运行，翻译功能仍然可用，但不会保存历史
- Web界面需要先构建（`npm run build`）才能访问
- 配置文件保存在 `~/.fanyi-config.json`
